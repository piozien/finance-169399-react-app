server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:postgresql://dpg-cvhe2jqqgecs73cqoqj0-a:5432/finance_pfi0
    username: finance_pfi0_user
    password: aWfnJ7f1l8K16TRuEhK3JSVrpnclMjWx
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
  security:
    user:
      name: ${ADMIN_USERNAME}
      password: ${ADMIN_PASSWORD}

app:
  cors:
    allowed-origins: https://finance-169399.netlify.app

logging:
  level:
    graduate:
      finance_dashboard: INFO
